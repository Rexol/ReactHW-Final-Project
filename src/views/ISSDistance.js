import React, { useState } from 'react'

function ISSDistance() {
    const [latitude, setLatitude] = useState(0);
    const [longtitude, setLongtitude] = useState(0);

    return (
        <></>
    );
}

export default ISSDistance